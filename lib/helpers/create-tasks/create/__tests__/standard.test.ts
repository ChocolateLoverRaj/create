import standard from '../standard'
import { readFile } from 'jsonfile'

it('standard, typescript, no react', async () => {
  const beforeWrite = []
  standard.fn('standard', {
    beforeWrite,
    data: {}
  } as any, true, 'none', false)
  rea
})
